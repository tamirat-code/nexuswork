import { Router } from "express";
import { placeholder } from "./verifications.controller.js";

const router = Router();

// TODO: replace with real routes for the "verifications" module.
router.get("/", placeholder);

export default router;
