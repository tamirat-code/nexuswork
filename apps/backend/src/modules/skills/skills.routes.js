import { Router } from "express";
import { placeholder } from "./skills.controller.js";

const router = Router();

// TODO: replace with real routes for the "skills" module.
router.get("/", placeholder);

export default router;
